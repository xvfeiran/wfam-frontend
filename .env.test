# 测试环境配置
# 此文件会提交到 git，构建测试镜像时通过 --build-arg BUILD_MODE=test 使用

VITE_BACKEND_URL=http://wfam-api.quality.tmp-service.bosch.com/aftermarket-parts-management-system/api/v1

# 微应用模式（子应用）：测试环境网关地址
VITE_GATEWAY_URL=https://aepgateway.quality.tmp-service.bosch.com/aftermarket-parts-management-system/api/v1

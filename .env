# 调试模式（?dev=1）：浏览器直接访问后端
VITE_BACKEND_URL=http://localhost:8102/aftermarket-parts-management-system/api/v1

# 微应用模式（子应用）：浏览器直接访问网关（生产构建前必须配置）
VITE_GATEWAY_URL=

# 报表分析/质量模块 API 地址
VITE_REPORT_API_URL=https://wfam-task.quality.tmp-service.bosch.com

# token 过期时重定向至 AEP 登录页（子应用模式生效）
VITE_AEP_LOGIN_URL=
